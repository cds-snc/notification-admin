FROM python:3.6-alpine

ENV PYTHONDONTWRITEBYTECODE 1

# Install deps
RUN apk add --no-cache build-base libxml2-dev libxslt-dev git nodejs npm g++ make libffi-dev chrome-chromedriver && rm -rf /var/cache/apk/*

CMD ["/bin/sh"]